import React from 'react'
import ReactDOM from 'react-dom'
import App from './examples/LogIn/App.jsx'

const container = document.getElementById('app');

// Create a root.
const root = ReactDOM.createRoot(container);
root.render(<App />)